---
name: slack-catchup
description: Alias for the Slack catch-up mode of draft-replies (merged 2026-09-22). Use when the owner says "slack catch-up" or "/slack-catchup" - then follow the draft-replies skill in Slack catch-up mode.
---

# Slack Catch-up (alias)

Merged into `.claude/skills/draft-replies/SKILL.md` on 2026-09-22, so the
drafting rules live in one place. Open that skill and follow its **Slack
catch-up** mode: step 1, the Slack part of step 2, then steps 3-6. The
checkpoint (`query.py slack since` / `slack mark-scanned`) is unchanged.

Kept as a pointer rather than removed (never-delete rule); it carries no rules
of its own, so it cannot drift.
