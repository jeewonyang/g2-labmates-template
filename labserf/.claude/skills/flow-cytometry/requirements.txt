# The pipeline needs only the scientific-Python basics. This user's conda base
# (Python 3.9, numpy 1.21, scipy 1.7, matplotlib 3.5) already satisfies it and
# produces byte-identical results to a modern 3.12 environment, so no dedicated
# environment is required. Listed here for a machine that lacks them.
numpy>=1.21
scipy>=1.7
matplotlib>=3.5
